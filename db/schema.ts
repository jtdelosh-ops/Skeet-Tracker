import { integer, sqliteTable, text, uniqueIndex } from "drizzle-orm/sqlite-core";
export const shoots = sqliteTable("shoots", { id: integer("id").primaryKey({ autoIncrement: true }), shootNumber: integer("shoot_number"), name: text("name").notNull(), date: text("date").notNull() });
export const eventScores = sqliteTable("event_scores", { id: integer("id").primaryKey({ autoIncrement: true }), shootId: integer("shoot_id").notNull().references(() => shoots.id, { onDelete: "cascade" }), event: text("event", { enum: ["12", "20", "28", "410", "doubles"] }).notNull(), broken: integer("broken").notNull(), targets: integer("targets").notNull(), classShot: text("class_shot") }, table => [uniqueIndex("idx_event_scores_shoot_event").on(table.shootId, table.event)]);
